# Development Flow
## 1. Start with server.ts — set up Express + Socket.io + MongoDB connection.

## 2. Add User and Message models — define schemas and interfaces.

## 3. Implement authController + authRoutes — JWT authentication.

## 4. Add chatSocket.ts — handle real‑time events and broadcast messages.

## 5. Create messageController + messageRoutes — fetch chat history.

## 6. Integrate authMiddleware — protect private routes.

## 7. Refactor and test — ensure types are consistent across layers.